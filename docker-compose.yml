services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: changeme
      POSTGRES_DB: chamatenis
    ports:
      - '5433:5432'
    volumes:
      - chamatenis_postgres_data:/var/lib/postgresql/data

volumes:
  chamatenis_postgres_data:
